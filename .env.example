# Lola Drip – Environment Variables Template
# Copy to .env.local and fill in values. Never commit .env.local.

# Required: Full site URL (no trailing slash)
NEXT_PUBLIC_SITE_URL=https://loladrip.com

# Database (PostgreSQL - use Vercel Postgres, Railway, or Supabase)
DATABASE_URL="postgresql://username:password@localhost:5432/loladrip?schema=public"

# Auth (NextAuth.js / Auth.js) - REQUIRED for auth to work
# Generate: npx auth secret  OR  openssl rand -base64 32
AUTH_SECRET=your-super-secret-key-change-this-in-production
# Optional: AUTH_URL for production (defaults to NEXTAUTH_URL)
NEXTAUTH_URL=http://localhost:3002

# Stripe (get from https://dashboard.stripe.com/apikeys)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
STRIPE_SECRET_KEY=sk_test_your_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Optional: Google Analytics 4 measurement ID
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Optional: API base URL (if using external services)
# NEXT_PUBLIC_API_URL=https://api.example.com

# Optional: Search console verification (add when ready)
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your-google-verification-code
# NEXT_PUBLIC_YANDEX_VERIFICATION=your-yandex-verification-code

# Virtual Try-On: Replicate API (get token from https://replicate.com/account/api-tokens)
# REPLICATE_API_TOKEN=your_replicate_token
